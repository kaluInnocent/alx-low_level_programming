/**
* swap_int - function sawps the values of two integers
* @a: first parameter to be supplied
* @b: second parameter to be supplied
* Return: returns nothing
*/
void swap_int(int *a, int *b)
{
	int temp = *a;
	*a = *b;
	*b = temp;

}
