#include "main.h"
/**
* mul - function multiplies two integers
* @a: first integer parameter
* @b: second integer parameter
* Return: return (0)
*/
int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
